import numpy as np

np.random.seed(12)

n = 0

def calculateFraction(n):
  if (n % 2 == 0): # even
    s = np.random.uniform(0,(n/2)/n,10000)


def calculateFraction(n):
  if n == 1:
    return 0.5
  else:
    return np.mean([average_heads(n-1) + 1./n, average_heads(n-1) - 1./n])

s = np.random.uniform(0,2,10000)
print samples.mean() # 0.4987
print samples.std() # 0.499998309997
